query CategoryMangas($categoryId: Int!) {
  category(id: $categoryId) {
    mangas {
      nodes {
        id
        title
        thumbnailUrl
        inLibrary
        sourceId
      }
    }
  }
}
