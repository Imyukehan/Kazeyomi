import SwiftUI

struct ReaderSettingsView: View {
    var body: some View {
        PlaceholderView("阅读器", description: "阅读器设置将逐步对齐 Sorayomi 实现")
    }
}

#Preview {
    NavigationStack {
        ReaderSettingsView()
    }
}
