import SwiftUI

struct DownloadsSettingsView: View {
    var body: some View {
        PlaceholderView("下载", description: "下载设置将逐步对齐 Sorayomi 实现")
    }
}

#Preview {
    NavigationStack {
        DownloadsSettingsView()
    }
}
