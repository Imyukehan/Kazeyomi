import SwiftUI

struct GeneralSettingsView: View {
    var body: some View {
        PlaceholderView("通用", description: "通用设置将逐步对齐 Sorayomi 实现")
    }
}

#Preview {
    NavigationStack {
        GeneralSettingsView()
    }
}
